---
interface Props {
	status: string;
}

const { status } = Astro.props;
---

<span class="rounded-full bg-[var(--badge-bg)] px-2.5 py-1 font-mono text-xs text-[var(--badge-text)]">
	{status}
</span>
